# C1 post-Oracle experiment plan — 2026-09-04

Status: **FROZEN PLAN / NO NEW SCIENTIFIC OUTCOME**  
Current paper archetype: **measurement / identification**  
Independent review verdict: `KEEP_MEASUREMENT_MOVE_REPAIR_DISCUSSION`  
Review conversation: `6a9a1dc5-b728-83e8-8852-2f7692a5bbd6`  
Review transcript SHA256: `cc08ce0415e728b69dc788084335dfd7050845b9c952cbe3d9f8743a95c01783`

## 1. Decision that changes the experiment program

The current C1 paper no longer depends on a successful PACTA-MSR method-effect experiment.

The completed scientific object is the stage-resolved measurement/identification study:

1. same-trajectory success/failure writer intervention creates durable branch-conditioned memory divergence;
2. forced fixed-evidence exposure demonstrates downstream leverage under supplied memory;
3. native source-item exposure remains common;
4. a stable first-action branch contrast is not supported on the frozen Shopping evidence;
5. native terminal transport is sparse, and the Reddit extension is sign-heterogeneous.

The strongest current operational conclusion is therefore:

> Durable branch-conditioned state divergence plus frequent native source-item exposure is insufficient to establish a stable branch-conditioned first-action distribution shift in the frozen C1 system.

Do **not** upgrade this to a claim that latent state relevance `R_t` was directly observed, that latent behavioral authority `A_t` was absent, or that selective authority is the uniquely identified missing mechanism.

PACTA-MSR remains a **prospective successor hypothesis** only. Fresh2/fresh3/fresh4 provider/source failures are not negative method results and do not block the current paper.

## 2. Track A — current C1 paper: mandatory work, zero new scientific provider calls

### A0. Freeze the completed evidence

No rerun, replacement, top-up, or outcome reclassification of the completed C1 measurements.

Preserve the current completed evidence ledger and the stage-specific semantics of `W`, `E`, `U`, `O`, and the forced-capacity side control `F`.

No scalar transport-efficiency ratio and no causal-mediation claim.

### A1. Build an evaluation-surface baseline table from existing evidence only

This is the baseline comparison appropriate to the current measurement paper. It requires no new model/environment execution.

Compare what a reviewer would conclude under five observation surfaces applied to the same frozen system:

| Evaluation surface | Observable | Coarse conclusion it can support | Failure mode / aliasing |
|---|---|---|---|
| Write-only | `W` | persistent update occurred | says nothing about downstream use |
| Retrieval-only | `E` | source item was available to policy | availability can be mistaken for uptake |
| Native endpoint-only | `O` | terminal effect is sparse/small on frozen panel | cannot localize where transport attenuated |
| Forced-only | `F` | supplied branch-specific memory can affect downstream behavior | bypasses native retrieval/transport |
| Stage-resolved C1 | `W,E,U,O` + side `F` | separates durable state, exposure, first decision, terminal effect, and latent capacity | current paper contribution |

This table is an **evaluation baseline / observation ablation**, not an algorithmic baseline ranking.

### A2. Main Figure 1 replacement

Create one evidence-status stage diagram:

`shared trajectory tau -> S/F writer fork -> Write -> Exposure -> First-action -> Terminal`

Place the frozen C1 evidence directly below the corresponding stages. Put forced fixed-evidence on a side bypass explicitly labeled:

`capacity control — not a native stage`

After the unsupported first-action boundary, add a small dashed box only:

`Prospective hypothesis: selective post-exposure authorization / PACTA-MSR — untested`

Do not show PACTA-MSR as a completed method block.

### A3. Claim/evidence audit

Before submission-level review, mechanically check that:

- `E` is called exposure/availability, not direct state relevance;
- unsupported `U` is called an unsupported stable first-action contrast, not proof of zero influence or zero authority;
- forced leverage is not inserted into the native chain;
- PACTA-MSR is Discussion/Future Work or Appendix protocol, not a completed main contribution;
- fresh2/fresh3/fresh4 failures are described only as realization/provider failures with zero method-effect authority;
- no statement says PACTA-MSR improves correctness, reward, or task performance.

### A4. Submission decision gate for the current paper

After A1-A3, run a fresh independent submission-level paper review.

Current-paper PASS condition: reviewer agrees that the completed measurement/identification contribution is coherent and not dependent on an unexecuted repair method.

A failure here should trigger manuscript/claim repair first, **not** automatic new provider/model experiments.

## 3. Track B — PACTA-MSR successor P0: optional, independent claim-expansion experiment

Track B is opened only if we deliberately choose to test the diagnosis-derived selective-transport hypothesis. It is not required to finish Track A.

### B0. Scientific question

On a frozen scientific-unit panel, does a selector based on stable matched-state success/failure branch sensitivity yield stronger **decision-level branch transport** than an equally sparse outcome-blind random authorization rule?

This P0 does **not** claim improved task correctness or reward. Stability is not correctness.

### B1. Arms

Keep the existing four-arm structure:

- `A0`: native raw branch memory at the matched revealed state;
- `A1`: existing state-conditioned binder / SCB always enabled;
- `A2`: rate-matched random authorization using a ranking frozen before shadow outcomes;
- `A3`: PACTA-MSR selective authorization.

The primary causal comparator remains `A3 - A2`; `A0` and `A1` are companion controls.

### B2. Preserve the developmental selector, but demote its interpretation

Retain

`gamma_i = min(B1_i,B2_i) - max(WS_i,WF_i)`

and `G_i = 1[gamma_i > 0]` as a **developmental qualification/localization statistic only**.

It may identify reproducible branch sensitivity. It does not identify usefulness, correctness, or reward improvement.

### B3. New hard statistical prerequisites before any successor scientific outcome

#### B3.1 Canonical action identity

Freeze a deterministic canonical-action compiler before shadow/final execution.

Primary MMD2 must operate on canonical structured action IDs rather than presentation-level strings. The compiler must remove transport/serialization-only variation while never outcome-conditionally merging actions. Raw exact strings remain a secondary sensitivity readout.

No P0 provider call is authorized until the canonicalizer has a zero-provider audit artifact and deterministic 100% replay on its frozen qualification corpus.

#### B3.2 Shadow/final sample independence

Shadow samples used to construct `G_i` and all final samples used to estimate `U_a` must be disjoint logical model calls with disjoint request/sample identifiers.

The final stage begins only after the full shadow receipt, `G`, opening count `K`, and rate-matched A2 membership have been content-addressed and frozen.

No shadow response may be reused as a final response.

#### B3.3 Nondegenerate selector opening

Hard qualification gate for Pilot8:

`2 <= K <= 6`.

If `K < 2` or `K > 6`, stop before final. Do not rescue by changing the threshold, reranking, adding units, or reusing sealed units.

#### B3.4 Scientific-unit inference

The scientific unit is the source/future unit; repeated model calls are measurements within a unit.

The P0 primary claim is explicitly a **frozen-panel effect**, not a population-general claim.

For unit-level `D_select_i`, add a prospectively frozen one-sided exact paired sign-flip test over the Pilot8 units. Report all unit-level values and the exact p-value. A population-general statement requires a later disjoint confirmatory panel and is not granted by P0.

### B4. Final metric and prospective gate

Retain the unbiased exact-match-kernel MMD2 / collision U-statistic after canonical action compilation:

`U_a = MMD_u^2(A_a^S, A_a^F)`

`D_select = U_A3 - U_A2`.

Finite-sample negative MMD2 values remain legal; no clipping at zero. Raw empirical TV is descriptive secondary only.

Before provider execution, rerun zero-provider simulation calibration for the **combined revised gate**, because adding the scientific-unit sign-flip requirement changes the operating characteristics of the old Q08 gate.

The prospective P0 PASS gate is:

1. all required final calls complete;
2. `2 <= K <= 6`;
3. `mean(D_select_i) >= 0.20`;
4. positive `D_select_i` count > negative count;
5. one-sided exact paired sign-flip `p <= 0.05`;
6. `mean(A3-A0) > 0` on the same canonical MMD2 scale;
7. `mean(A3-A1) >= 0` on the same canonical MMD2 scale.

Any failure is a P0 HOLD/FAIL under the frozen rule. Do not tune the gate after outcome inspection.

## 4. Track B provider/substrate qualification — repair the actual fresh4 failure mode first

Fresh4 is permanently retired as a consumed/incomplete source epoch. Do not continue, replace, or top up it.

The observed fresh4 stop occurred on the first scientific source unit after 27 CodingPlan requests, with model content observed but `controlled_output_live_timeout`; writer/binder/probe/shadow/final remained zero. This is provider/transport realization evidence, not method-effect evidence.

Before creating or consuming another fresh scientific pool, require a new non-scientific longevity qualification under the exact intended provider bridge envelope:

### Q0.9 Long-horizon controlled-output qualification

- no SWE-bench scientific source task;
- growing serialized conversation, as in source acquisition;
- same allowed controlled-output tool and no host tools;
- same timeout, model profile, and one-model-round-per-logical-output envelope;
- two independent synthetic episodes;
- each episode must complete **64 logical outputs** without retry, timeout, truncation, or prohibited tool use; 64 is intentionally far beyond the old 8-turn Q0.8 smoke and more than twice the observed scientific failure horizon at logical call 27, while remaining a qualification stress rather than a 250-step scientific trajectory;
- turns 1-63 must return the required source-like action format with deterministic synthetic observations appended;
- turn 64 must complete the frozen ordinary-JSON finalization object;
- every logical output must have a content-addressed provider receipt.

If either 64-turn episode fails, keep the AtomGit/Qwen3.8 provider lane on HOLD and do not consume another scientific source pool. The scientific plan may then either remain measurement-only or open a separately preregistered provider/model lane; do not silently substitute providers inside the same frozen experiment.

## 5. Track B execution order after all zero-provider repairs pass

Only if B3/B4 and Q0.9 pass:

1. freeze a wholly disjoint fresh source/future pool and exact image/runtime support;
2. acquire all source trajectories under the all-or-stop source gate;
3. run deterministic matched-state reveal/probe on the prospectively frozen Pilot8 only;
4. run writer twins;
5. run binder contexts;
6. run disjoint developmental shadow samples;
7. freeze `G`, `K`, and A2 rate-matched membership;
8. stop if selector qualification fails;
9. run disjoint final samples for A0-A3;
10. compute canonical-action MMD2, unit-level contrasts, exact sign-flip test, and frozen-panel adjudication;
11. keep sealed units untouched in P0.

No terminal/task-utility claim, second model, or broad benchmark expansion in this P0.

## 6. Optional P1 only after a genuine P0 PASS

P1 is not authorized by this plan. It becomes worth designing only if P0 passes all frozen gates.

If opened later, its purpose is claim expansion rather than rescue:

- fresh disjoint confirmatory scientific-unit panel;
- population-oriented uncertainty/inference frozen before outcomes;
- one strong contemporary decision-time arbitration/gating baseline if a reproducible implementation can be matched fairly;
- task utility/correctness may be added as a separate endpoint only if the paper wants to claim a practical repair rather than selective transport;
- at most one additional backbone/executor initially; broad model scaling is not the first remedy.

A P0 failure does not trigger P1.

## 7. Resource priority

**Priority 1:** finish Track A with zero new scientific provider calls.  
**Priority 2:** only if we want a separate method-expansion result, implement B3 and Q0.9 as zero-provider/non-scientific gates.  
**Priority 3:** consume a new scientific source pool only after those gates pass.

This ordering prevents provider instability from blocking a scientifically complete measurement paper and prevents an unexecuted successor controller from carrying unsupported novelty claims.
