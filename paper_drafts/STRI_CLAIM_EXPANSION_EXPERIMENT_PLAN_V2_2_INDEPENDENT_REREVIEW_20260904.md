# Independent adversarial re-review — STRI Claim-Expansion Experiment Plan V2.2

Date: 2026-09-04
Reviewer role: same independent senior ICLR/NeurIPS/ICML agent-systems methodology reviewer that adjudicated V2/V2.1
Reviewer substrate: ChatGPT Web via Oracle Browser on host 52
Model: GPT-5.6 Sol
Thinking effort: Extra High
Oracle session: `continue-as-the-same-independen-4`
Conversation: `https://chatgpt.com/g/g-p-6a6ad664d6508191bff6ecf4fde868f0-agent/c/6a9a412a-c97c-83ee-b3e7-add29dd8d4e0`
Scientific outcomes opened: none
Execution authority: CLOSED

## Frozen object

Plan commit reviewed:

`0ff113557201d003dda3d8e72e98c5cf88103074`

Plan file:

`paper_drafts/STRI_CLAIM_EXPANSION_EXPERIMENT_PLAN_V2_20260904.md`

Plan SHA256:

`0f97ff308a9eae854171d6b0b992fef57dd08e3bb039eecdfe74458b65457dac`

V2.2 adds zero provider/model trajectories and changes no workload. It tightens semantic equivalence, the complete checkpoint fork, P0-vs-P1 gates, arm-indexed dynamic recovery/eligibility/censoring, and conditional interpretation.

## Reviewer answer

### A–J audit

**A — PASS.** `X_i` now defines the complete representation-independent fork through \(t_0^-\), including paired reconstruction/access/ranking/routing/loading/actor stochastic state; arm divergence begins only with representation realization and its descendants.

**B — PASS.** Source acquisition is finite and non-adaptive, checkpoint eligibility is pre-access and outcome-blind, and both checkpoint and primitive-pair overqualification are resolved by preregistered hashes rather than retrieval or downstream behavior.

**C — PASS.** `G_sem` closes the important semantic-equivalence gap: A+B versus A/B may alter only container boundaries while primitive bytes, multiplicity, ordering, dependencies, and composition relations remain invariant. This is a genuine repartition rather than a covert program rewrite.

**D — PASS.** O/R/I correctly reconstruct representation-derived state arm-specifically, while I isolates ID/reconstruction nuisance under unchanged package allocation; `D_ID>0` remains a sufficiently strict falsification gate.

**E — PASS.** The native physical-package ranker is preserved and only admission capacity is normalized semantically. Longest-prefix admission avoids both package-count confounding and replacement of the mechanism by a semantic reranker; unused slack and non-binding `C_vis` remain appropriately controlled.

**F — PASS.** `PASS_P0_LOCAL` now has the correct claim-specific meaning: valid non-clone semantic exposure divergence replicated across at least two independent source trajectories, without incorrectly requiring loss-bearing cases needed only by P1.

**G — PASS.** `OPEN_P1` is now logically distinct and exactly matched to its estimand: four \(L_0\neq\varnothing\) checkpoints, valid placebos, at least two source trajectories, deterministic hash selection if overqualified, and no post hoc top-up.

**H — PASS.** O/S/P identify the intended regimes. S removes all treatment-controlled Repacked state after \(t_0\) while preserving endogenous consequences of the initial exposure; P maintains Repacked treatment state. This cleanly separates one-shot from persistent representation exposure without artificial state alignment.

**I — PASS.** Arm-indexed \(\tau_{a,v}\), \(R_{a,j}\), candidate-support eligibility, and explicit separation of `ELIGIBLE_NOT_EXPOSED` from `NOT_ELIGIBLE` remove the main post-\(t_0\) attribution ambiguity. `TERMINATION` versus `NO_LATER_ACCESS` also prevents absence of reacquisition from being automatically relabeled as repeated representation suppression.

**J — PASS.** P1 conclusions are explicitly conditional on prospectively governed P0-qualified loss divergences, heterogeneity stops rather than expands the block, P2 remains claim-contingent rather than automatic, and the 0/8-source + 24-P0 + 12-P1 + optional-24-P2 structure remains proportionate. Existing structural baselines plus O/R/I and O/S/P are sufficient; MMR/diversity remains positioning rather than a required causal baseline.

### (1) Is V2.2 stronger without buying validity by volume?

**Yes.** This is a genuine methodological strengthening with zero evidentiary inflation.

The five changes each remove a different ambiguity:

- `G_sem` upgrades “same primitive multiset” to actual semantic-program equivalence.
- The \(X_i\) definition upgrades checkpoint matching to a complete causal fork.
- `PASS_P0_LOCAL` versus `OPEN_P1` prevents the P0 claim from being accidentally defined by the downstream P1 recovery estimand.
- Arm-indexed eligibility/recovery prevents post-treatment trajectory divergence from being misclassified as persistent suppression.
- Explicit conditional interpretation prevents the four selected P1 checkpoints from being generalized to arbitrary checkpoints.

None requires another model, trajectory, checkpoint, seed, benchmark, or baseline. That is exactly the right direction for an already submission-ready paper considering optional claim expansion.

### (2) Did any V2.2 change alter the intended estimand or introduce a new confound?

**No.**

The P0 estimand remains the effect of physical package repartition on canonical semantic exposure under a native physical-package ranker and representation-neutral capacity.

The P1 estimand remains recovery/persistence under O/S/P regimes conditional on a demonstrated local loss-bearing divergence. Arm indexing does not change that estimand; it makes its observational clock and risk/eligibility set well-defined after trajectories diverge.

`G_sem` likewise does not narrow the treatment into a different intervention. It simply makes explicit what “semantics-preserving repartition” already had to mean for the original claim to be identified.

The treatment-state reset also does not erase legitimate mediation: S retains every endogenous historical consequence of its \(t_0\) Repacked exposure. Only the representation intervention itself is discontinued.

I find no newly introduced confound.

### (3) Smallest remaining verdict-changing fix

**None.**

The V2.1 blockers remain closed, and the V2.2 tightenings improve identification rather than changing the scientific object. No additional execution, baseline, model breadth, checkpoint count, or seed count is required before a separately controlled execution-authority decision.

**Verdict-changing required fixes: none.**

PASS_MINIMUM_CLAIM_EXPANSION_DESIGN
