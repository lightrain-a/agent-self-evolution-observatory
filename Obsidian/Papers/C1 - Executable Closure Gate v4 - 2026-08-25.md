---
knowledge_version: C1-CLOSURE-v4-20260825
paper_id: D2-PAPER-PROXY-REWARD-MEMORY-VARIANCE
paper_title: Reward Errors Change Memory Before They Change Policy
status: CBRG_CURRENT_EXTENSION_STOP_MERGE_MEASUREMENT_PAPER_RETAINED
supersedes: C1 - Executable Closure Gate v3 - 2026-08-24
scientific_provider_calls_added: 0
scientific_gpu_runs_added: 0
---

# C1 · Executable Closure Gate v4

Tags: #C1 #agent-memory #reward-error #mechanism #reviewer-gate #D0 #closure

## 1min 结论

C1 的方法扩展已经到达一个合法的科学停止点：

> **当前冻结的 CBRG 扩展 STOP/MERGE；C1 的阶段化识别/测量论文保留。**

这不是因为实验环境失败，也不是因为 C1 现象被证伪。停止原因更具体：CBRG 若要从“同轨迹 branch contrast”走到“证据化触发权限”，必须有一个独立、outcome-independent、可输出 `SUPPORTED / CONTRADICTED / UNVERIFIABLE` 的语义有效性 adjudicator。当前冻结的本地资产和仓库库存里都没有这样的合格对象，也没有对应的 task-specific pre-outcome qualification receipt。

因此不能用 MiniLM 相似度、lexical locator、generic NLI 名称、或临时自写规则替代这一层。

## 已保留的科学结果

以下结果不因方法扩展停止而失效：

- C1 的阶段链：`reward-conditioned write -> retrieval exposure -> branch-specific uptake -> outcome`；
- 24/24 frozen S/F pairs 的 operational same-trajectory intervention identity；
- `D_tau = Delta(W_success(tau), W_failure(tau))` 作为 operational branch-contrast object；
- 423/423 directional branch-contrast units 可重复编译；
- 397/423 units 具有 nonzero exact lexical evidence anchor；
- 26/423 units 保持 fail-closed unlocated；
- semantic validity 仍为 0/423，branch authority 仍为 0；
- atom-level causal purity 仍未获认证，因为当前 24 pairs 没有 explicit decoding seed / same-condition noise-floor replication。

因此 C1 可以继续作为“reward-induced memory divergence 在哪一阶段停止传播”的 identification / measurement paper，而不能把 CBRG 写成已经成立的方法贡献。

## 为什么从 HOLD 进入 STOP/MERGE

v3 的 D0-B2 先得到：

- local entailment+contradiction-labelled classifier configs = **0**；
- C1 semantic qualification receipts = **0**；
- qualified local adjudicators = **0**；
- MiniLM = embedding-similarity baseline only；
- semantic decisions = **0/423**；
- provider calls = **0**；
- GPU runs = **0**。

v4 没有因此发明一个新 verifier，而是继续做 bounded repository inventory。结果：

- bounded text/code artifacts scanned = **597**；
- all-three-state vocabulary hits = **12**；
- entailment+contradiction vocabulary hits = **6**；
- external executable adjudicator candidates = **0**；
- external semantic qualification receipts = **0**；
- admissible qualified repository adjudicators = **0**。

12/6 个词汇命中都来自 C1 自己的 gate / receipt / program。它们只能定义“什么算合格”，不能反过来证明自己就是合格 verifier。

对应 content-addressed closure receipt：

- artifact: `paper_drafts/c1-proxy-reward-stanford-r3-20260824/cbrg-d0b2-adjudicator-inventory-closure-20260825.json`
- SHA-256: `716b5aeaed83a39af8d52938c8fb98f0832b02477f5f021145457984e54873cc`
- runner: `paper_drafts/c1-proxy-reward-stanford-r3-20260824/audit_cbrg_d0b2_adjudicator_inventory_closure.py`
- runner SHA-256: `a354ae88093b8b6f2137202f167889532609407bcd97a09d0254ff14c79f1675`

该 runner 连续重放两次得到完全相同的 artifact SHA；其 inventory 显式排除自身输出，并避免把 C1 gate/program SHA 写入自己的 closure receipt，从而消除自引用哈希循环。

## 当前终止路由

- `D0-A` → complete
- `D0-B0 receipt envelope` → complete
- `D0-B1a operational contrast` → GO
- `D0-B1b atom-level causal purity` → HOLD
- `D0-B1c compiler` → GO
- `D0-B1c exact locator` → PARTIAL 397/423, 26 fail-closed
- `D0-B2 local semantic readiness` → HOLD, no qualified adjudicator
- `D0-B2 bounded repository inventory` → EXHAUSTED for current frozen zero-call closure
- `CBRG current extension` → **STOP/MERGE**
- `fresh collision / provider method experiment` → **NOT REACHED / NOT AUTHORIZED**
- `C1 identification/measurement paper` → **RETAIN**

## 第 7 个 reviewer gate

Research OS 现在同时保留两个事实：

1. 历史 `C1_EXECUTABLE_CLOSURE_REVIEWER_GATE_V3` 曾判定该残差具有 **D0 design eligibility**；
2. 新 terminal closure gate 覆盖这个历史资格，使 **current D0 design eligibility = false**。

这样后续 Agent 不会因为看到旧的 D0 `GO` 就继续开 provider/GPU experiment。

terminal gate 必须保证：

- method-extension STOP 不得改写成 C1 scientific failure；
- method-extension STOP 不得使既有 measurement evidence 失效；
- generic NLI availability 不得自动 reopen；
- renamed similarity / common residual 不得自动 reopen；
- self-authored post-outcome verifier 不得自动 reopen；
- STOP receipt 不得授予 scientific / experiment / provider / GPU / claim-expansion / submission authority。

## Reopen contract

当前 CBRG 扩展只有在以下四项 **全部**出现后才允许重新进入 qualification：

1. 新的 content-addressed adjudicator candidate，显式输出 `SUPPORTED / CONTRADICTED / UNVERIFIABLE`；
2. independent task-specific **pre-outcome C1 qualification receipt**，绑定 exact adjudicator SHA；
3. 在相同 frozen information 上证明它不等价于 lexical locator、embedding similarity 或 semantic applicability；
4. fresh closest-work collision clearance 仍表明 exact residual 未被 subsume。

任何单独一项都不构成 reopen authority。

## 论文路线

CBRG 不再进入当前稿件的方法主贡献。C1 返回并强化以下闭环：

`reward-label intervention -> durable write divergence -> forced downstream leverage -> native transport boundary -> first-action uptake localization -> cross-domain heterogeneity -> evaluation/design implication`

关键写作边界：

> **memory divergence is not behavioral divergence, and observed branch-specific memory content is not validated behavioral authority.**

CBRG 的失败开发过程可以作为内部 Research Memory / supplement-level development note，但不能包装成已验证 solution。

## Authority snapshot

- scientific provider calls added in v4: **0**；
- GPU scientific runs added in v4: **0**；
- scientific authority: **false**；
- experiment authority: **false**；
- provider authority: **false**；
- GPU authority: **false**；
- claim-expansion authority: **false**；
- submission authority: **false**；
- active next route: **paper-only strengthening of the retained C1 identification/measurement story**；
- current CBRG provider experiment: **not authorized**。
