# E2-R17 State-Compiler Bridge V4-R2 — factorial-primary orthogonal-gate pre-execution repair

Status: **REVISED_AFTER_V4_R1_GPT56_PREEXECUTION_REVIEW / PROPOSAL_ONLY / ZERO_PROVIDER / NO_EXECUTION_AUTHORITY**
Base canonical scientific lineage: `0be443c0` (`PRIMARY_STATE_GENERATION_BOTTLENECK / PARK_SEMANTIC_TRANSFER`)
Repair branch: `research/e2-r17-state-compiler-bridge-v4-review-repair-20260903`

V4-R2 preserves the V4-R1 generator-factor main effect and applies the final verdict-changing fixes from the independent combined V4-R1/M3R2 pre-execution review. It remains a zero-provider prospective repair only. It adds **zero** provider calls, zero tasks, zero model families, and zero E3 authority. The changes are limited to universal state-SHA equivalence, stronger bounds on the Q3 stochastic interpretation, and explicit FF4-only scope for Q2 generic-control evidence.

## 0. V3 review provenance and exact blockers

The V3 prospective brief was independently reviewed on ChatGPT web with GPT-5.6 Sol / Extra High.

- V3 brief SHA256: `fb52e02cfbc83441249ea704c29b0cd34abc4ce7a1f74390c80f41434c990057`
- V3 Oracle output SHA256: `a05cc1dd339ba094419bcae912018c12e7346f3b65f348a9a3b818636145a021`
- Oracle session: `e2-r17-bridge-v3-final`
- Verdict: `REVISE_BEFORE_STAGE_A`

The reviewer accepted the central V3 pivot:

1. generator validation no longer depends on First-Fail superiority;
2. same-evidence `FF4_FREE_A/B` is the right repeated-synthesis object;
3. `SCOPE_MATCHED_GENERIC_MAX` is directionally the right stronger falsifier.

It identified five remaining protocol defects:

1. raw complete-method gates and generic-control interpretation were still conjunctively entangled, making the weaker canonicalization decision branch logically unreachable;
2. the primary declared first-deployed-realization estimand was incorrectly replaced in VALIDATION by `G_F_AB`;
3. `D_U-D_A` compared noncommensurate disagreement quantities;
4. byte-identical states could receive different actor realizations and manufacture a causal control contrast;
5. the FF4 score wording and the information status of scope scalar `k` were overstated.

V4 repaired exactly these issues. V4-R1 then made Q1 the balanced generator-factor main effect. A fresh independent pre-execution review of V4-R1 returned `REVISE_BEFORE_STAGE_A` and required three further fixes, all applied here:

1. the squared-propensity identity for `D_X-D_A` is stated only under conditionally iid/independent stationary actor realizations, not exchangeability alone; absent that assumption Q3 is bounded to observed excess disagreement;
2. state-SHA equivalence applies to **all** arms, including FREE↔COMP and FREE/COMP↔generic collisions, not only deterministic-arm pairs;
3. Q2 generic controls are explicitly FF4-specific and cannot be used to explain or mediate the balanced Q1 main effect.

Independent review receipt: `generated/e2-r17-v4r1-m3r2-preexecution-oracle-review-20260903.json`.

## 1. Paper hierarchy after canonical primary-line adjudication

The paper must keep four questions orthogonal.

### Q1 — complete state-generation method effect — PRIMARY

Across the two frozen evidence-source cells of the balanced 2×2, does the deterministic typed compiler produce more useful persistent states than the native free-form state generator, using the **first prospectively deployed** FF4 FREE realization?

This is the current primary paper line. Its stream-level estimand is the equal-weight generator-factor main effect:

`G_MAIN,A(s) = 0.5 * { [J_s(W_COMP)-J_s(W_FREE)] + [J_s(FF4_COMP)-J_s(FF4_FREE_A)] }`.

Thus Q1 asks whether the evidence→persistent-state conversion interface matters **averaged equally across Winner and FF4 evidence**. It does not require First-Fail to be superior to Winner, does not require an FF4-specific compiler advantage, and does not use the evidence×generator interaction as a gate.

### Q2 — FF4 trajectory-conditioned diagnosis interpretation — SECONDARY METHOD SUBSTANCE

Within the **FF4 compiler cell only**, does `FF4_COMP` outperform trajectory-text-blind generic canonicalization controls, including a control informed by repair-block cardinality?

This classifies whether the FF4 compiler branch shows method substance beyond generic/scope canonicalization. It does not decide whether the balanced Q1 complete-method effect exists, and it cannot explain or mediate a Q1 main effect that may be driven partly or entirely by the Winner cell.

### Q3 — observed same-evidence state-realization excess disagreement — SECONDARY MECHANISM

Under byte-identical FF4 evidence, do two separately produced FREE persistent states show greater **observed cross-state actor-outcome disagreement** than the observed within-frozen-state actor disagreement?

This is a prospective descriptive/localizing comparison. Under the additional model assumption that actor executions are conditionally iid/independent and stationary given frozen state, task, model, and runtime, the expectation of the statistic has a squared success-propensity interpretation. Because hidden provider randomness cannot be proven independent from logs alone, the protocol's unconditional claim is the observed excess-disagreement statement. Even under the stronger model, V4-R2 does **not** call it a population variance-component estimate and does **not** claim that variance reduction is the demonstrated causal mediator of compiler utility.

### Q4 — rejected-source / Search-Projection moderator — PARKED SECONDARY

Does First-Fail evidence outperform Winner evidence under the compiler, and is there evidence×generator interaction?

This remains scientifically useful but cannot veto Q1-Q3. It is the bridge to the parked Semantic-Transfer/Search-Projection moderator line, not the current paper headline.

## 2. Unchanged 2×2 scientific object

For each frozen stream, search/acting first produces and seals eight K=8 pools. Acting is identical across learning cells and uses the deterministic verifier-selected served winner.

The prospective 2×2 remains:

| Evidence package | Native FREE state generation | Typed constrained compiler |
|---|---|---|
| Winner | `W_FREE` | `W_COMP` |
| First-Fail-4 | `FF4_FREE_A` | `FF4_COMP` |

`FF4_FREE_A` is the first prospectively produced FREE state and is the deployed-realization reference for the primary complete-method estimand.

On VALIDATION only, exactly one additional state `FF4_FREE_B` is prospectively synthesized from the **same byte-identical FF4 evidence package**. B is never a retry, never replaces A, is generated regardless of A outcome, and no third draw exists. V4 calls A/B **separate provider realizations**, not guaranteed statistically independent draws.

The information-parity, runtime, retry-zero, evidence rendering, task split, heldout separation, state-budget measurements, and compiler hidden-metadata prohibitions remain as in the V3 prospective design.

## 3. Unchanged fresh development suite and stage separation

The bridge still uses 120 fresh task artifacts:

- 12 independent development streams;
- 8 update tasks per stream = 96 update tasks;
- 12 disjoint SCREEN heldout tasks;
- 12 different disjoint VALIDATION heldout tasks;
- six streams frozen to SCREEN and six to VALIDATION before provider execution.

No S1 outcome-selected task and no untouched E3 task enters the suite. Validation remains sealed until the raw generator SCREEN gate passes. SCREEN can never be used to alter the compiler vocabulary, diagnosis rules, thresholds, task split, controls, or V4 statistics.

## 4. FF4 evidence semantics and corrected score statement

For a stream, a pool is FF4-eligible iff it is mixed: at least one success and at least one failure. The failed evidence unit is the frozen deterministic first failed nonwinner. A stage is support-qualified only when every required frozen stream has at least four mixed pools.

Exactly four eligible pools are selected by the frozen FF4 hash and replaced; the other four pools retain the same served-winner evidence as Winner.

Therefore FF4 guarantees:

> **four forced failed replacements plus the four unchanged winner scores**.

It does **not** guarantee a universal four-failure/four-success score vector, because an unchanged winner score can itself be zero in an all-failure pool.

No stream/task/model/K replacement is permitted after support inspection.

## 5. Compiler and generic controls

The frozen compiler primitive vocabulary remains:

- `VERIFY_OUTPUT`;
- `COMPLETE_WORKFLOW`;
- `RECOVER_TOOL_ERROR`.

The compiler may use only learner-visible trajectory text plus the selected binary verifier score. It cannot use family, arm, projection, task identity, golden answers, heldout outcomes, or historical scientific outcomes.

### 5.1 `SCORE_ONLY_GENERIC_MAX`

Receives the exact ordered eight selected binary scores and **no trajectory text**. If any selected score is zero it emits the maximal nonredundant generic v1 surface `COMPLETE_WORKFLOW + RECOVER_TOOL_ERROR`; all-success returns S0.

It tests whether score/failure-count information plus maximal generic evaluator-aligned workflow advice already explains the effect.

### 5.2 `SCOPE_MATCHED_GENERIC_MAX`

Receives:

- the same ordered eight selected binary scores;
- scalar `k` = number of nonredundant repair blocks rendered by the typed compiler;
- no trajectory text;
- no repair primitive identity.

Frozen mapping:

- `k=0` -> S0;
- `k=1` -> generic `COMPLETE_WORKFLOW`;
- `k=2` -> generic `COMPLETE_WORKFLOW + RECOVER_TOOL_ERROR`.

The paper must describe this control exactly as:

> **trajectory-text-blind but diagnosis-cardinality-informed**.

`k` is a trajectory-derived repair-cardinality side channel. Passing this control supports benefit **beyond repair-block cardinality/scope**; it does not identify a pure semantic-diagnosis effect independent of every state-size or diagnosis-derived variable.

## 6. Universal state-SHA equivalence classes — mandatory before actor evaluation

Persistent state is the treatment. Therefore byte-identical states cannot be assigned a nonzero state-treatment effect merely because downstream actor executions are stochastic.

Before actor evaluation, construct equivalence classes by full persistent-state SHA256 **across every arm in the stage**: `W_FREE`, `W_COMP`, `FF4_FREE_A`, `FF4_FREE_B` when present, `FF4_COMP`, `SCORE_ONLY_GENERIC_MAX`, and `SCOPE_MATCHED_GENERIC_MAX`.

Rules:

1. whenever **any two arms** have identical state SHA, evaluate the unique state once under the frozen task/replicate schedule and reuse that observation for every aliased arm;
2. every state-level contrast between identical-SHA arms is exactly zero, including FREE↔COMP, COMP↔generic, FREE↔generic, or generic↔generic collisions;
3. if `FF4_FREE_A` and `FF4_FREE_B` produce byte-identical states, their state-realization contrast is exactly zero; B cannot acquire apparent realization difference from a separate actor draw;
4. `J_s(arm)` is defined on the state-SHA equivalence class observation, so `G_W`, `G_F,A`, `G_MAIN,A`, `C_SCORE`, `C_SCOPE`, and all sensitivity contrasts inherit alias collapse before subtraction;
5. if aliasing removes a duplicate actor execution, the resulting cost reduction is allowed; no replacement actor call may be spent elsewhere.

The analysis helpers independently enforce exact-zero pairwise contrast for identical SHA even if duplicate execution accidentally occurs.

## 7. SCREEN — raw method gate separated from interpretation

SCREEN evaluates the frozen primary 2×2 and both deterministic generic controls on the six SCREEN streams and 12 SCREEN heldout tasks.

For stream `s`, define the two within-source generator contrasts:

- `G_W(s) = J_s(W_COMP) - J_s(W_FREE)`;
- `G_F,A(s) = J_s(FF4_COMP) - J_s(FF4_FREE_A)`.

The primary generator-factor estimand is:

`G_MAIN,A(s) = 0.5 * [G_W(s) + G_F,A(s)]`.

The equal 0.5/0.5 weighting is frozen because the design is a balanced 2×2 and the paper's primary question is the generator factor rather than one evidence source. `G_W` and `G_F,A` are reported separately as directional components, but neither alone gates Q1.

### 7.1 `RAW_GENERATOR_SCREEN_PASS` — only Q1

Requires:

1. mean `G_MAIN,A > 0` across the six SCREEN streams;
2. `G_MAIN,A > 0` on at least 4/6 streams;
3. no catastrophic technical failure, evidence-parity violation, state-SHA accounting violation, or frozen-protocol drift.

**Generic controls do not enter this gate.**
**First-Fail superiority does not enter this gate.**
**Realization localization does not enter this gate.**

Only this raw gate determines whether VALIDATION may open for Q1.

### 7.2 SCREEN FF4 diagnosis-control classification — Q2 only

Define state-SHA-aware **FF4-only** contrasts:

- `C_SCORE = J(FF4_COMP) - J(SCORE_ONLY_GENERIC_MAX)`;
- `C_SCOPE = J(FF4_COMP) - J(SCOPE_MATCHED_GENERIC_MAX)`.

Interpret the two frozen mean contrasts without altering `RAW_GENERATOR_SCREEN_PASS`:

- mean `C_SCORE > 0` and mean `C_SCOPE > 0` -> `SCREEN_FF4_TRAJECTORY_CONDITIONED_DIAGNOSIS_PROMISING`;
- mean `C_SCORE > 0` but mean `C_SCOPE <= 0` -> `SCREEN_FF4_SCOPE_OR_SPARSITY_CANONICALIZATION_ONLY`;
- mean `C_SCORE <= 0` -> `SCREEN_FF4_GENERIC_CANONICALIZATION_NOT_REJECTED`.

These labels cannot close VALIDATION after the raw generator gate passes. They classify method substance **within the FF4 branch only**. They do not explain the Winner contribution to `G_MAIN,A` and must not be presented as a mediator or mechanism proof for the balanced Q1 main effect.

### 7.3 Rejected-source SCREEN classification — Q4 only

Using the frozen 2×2, record:

- `S_C = J(FF4_COMP)-J(W_COMP)`;
- interaction `I = [FF4_COMP-FF4_FREE_A] - [W_COMP-W_FREE]`.

The rejected-source hypothesis is promising only if the predeclared directional conditions pass. Failure sets `REJECTED_SOURCE_SCREEN_FAIL` but cannot veto raw generator VALIDATION.

## 8. VALIDATION — preserve the declared first-deployed estimand

If and only if `RAW_GENERATOR_SCREEN_PASS` holds, VALIDATION opens with all code, states, controls, metrics and thresholds frozen.

### 8.1 Primary complete-method confirmation — Q1

The primary validation contrast remains the same frozen generator-factor main effect used at SCREEN:

`G_MAIN,A(s) = 0.5 * { [J_s(W_COMP)-J_s(W_FREE)] + [J_s(FF4_COMP)-J_s(FF4_FREE_A)] }`.

`RAW_GENERATOR_VALIDATION_PASS` requires:

1. mean `G_MAIN,A > 0` across six VALIDATION streams;
2. `G_MAIN,A > 0` on at least 4/6 streams;
3. no technical, evidence-parity, SHA-aliasing, exactly-once, or protocol violation.

`FF4_FREE_B` does **not** enter this gate.

This preserves the first-prospectively-deployed FREE realization while making the state-generation headline a true generator-factor effect across the two frozen evidence sources. The FF4-specific contrast remains reported but is not the paper-defining estimand.

### 8.2 Realization-averaged sensitivity — descriptive/prespecified, never primary

Also report the FF4-specific sensitivity:

`G_F,AB(s) = J_s(FF4_COMP) - 0.5*[J_s(FF4_FREE_A)+J_s(FF4_FREE_B)]`,

and the corresponding generator-factor sensitivity:

`G_MAIN,AB(s) = 0.5 * { G_W(s) + G_F,AB(s) }`.

These are prespecified two-realization sensitivities approximating an average over two FF4 FREE realizations. They do not replace `G_MAIN,A`, do not gate Q1, and cannot rescue a failed raw generator result.

If `G_MAIN,A` passes but `G_MAIN,AB` is weak or changes sign, the paper must state that the observed first-deployment generator-factor effect is sensitive to alternative FF4 FREE-state realization. It may not silently change estimands.

## 9. Observed same-evidence state-realization excess disagreement — Q3

On the six pre-frozen VALIDATION variance tasks, retain exactly two actor realizations for each of `FF4_FREE_A`, `FF4_FREE_B`, and `FF4_COMP`; ordinary validation provides replicate 1 and exactly one preauthorized replicate 2 is executed regardless of replicate-1 outcome. No third execution exists.

For each stream and task define FREE outcomes `(A1,A2,B1,B2)`.

### 9.1 Cross-state disagreement

`D_X(s) = 1/4 * mean_q [ |A1-B1| + |A1-B2| + |A2-B1| + |A2-B2| ]`.

### 9.2 Within-state actor disagreement

`D_A(s) = 1/2 * mean_q [ |A1-A2| + |B1-B2| ]`.

### 9.3 Excess state-realization disagreement

`E_REAL(s) = D_X(s) - D_A(s)`.

For binary outcomes, **if** actor executions are conditionally iid/independent and stationary given frozen state, task, resolved model, and runtime, with success probabilities `p_A(q), p_B(q)`, then:

`E[E_REAL] = mean_q [p_A(q)-p_B(q)]^2`.

Fresh/reset task runtimes, separate actor calls, no carried conversation/context, retry zero, and fixed model/runtime identity are frozen execution invariants intended to support that model. Hidden provider randomness or shared service-level dependence cannot be proven independent from these logs, so the squared-propensity identity is explicitly a model-based interpretation rather than an unconditional identification claim. Without that assumption, `E_REAL` remains an observed cross-state-minus-within-state disagreement statistic.

If state SHA(A) == state SHA(B), define `E_REAL(s)=0` exactly.

`OBSERVED_STATE_REALIZATION_EXCESS_PASS` requires:

1. mean `E_REAL > 0` across six VALIDATION streams;
2. `E_REAL > 0` on at least 4/6 streams.

This directional development gate concerns the **observed** excess statistic. It is not a p-value or latent-propensity identification gate.

A pass unconditionally supports only:

> separately realized FREE persistent states from byte-identical evidence show greater observed cross-state actor-outcome disagreement than observed within-frozen-state actor disagreement in this frozen development design.

Only under the additional conditionally iid/stationary actor model may this be interpreted as evidence of differing latent success propensities. It does **not** establish a population variance component and does **not** prove that variance reduction causally mediates compiler utility.

Compiler actor disagreement remains a reported diagnostic, not a component subtracted from `E_REAL`.

## 10. VALIDATION FF4 generic-control classification — Q2 only

After the raw validation gate is adjudicated, independently classify the state-SHA-aware **FF4-only** `C_SCORE` and `C_SCOPE` means exactly as at SCREEN:

- both positive -> `FF4_TRAJECTORY_CONDITIONED_TYPED_DIAGNOSIS_SUPPORTED_BEYOND_REPAIR_CARDINALITY`;
- score positive / scope nonpositive -> `FF4_SCOPE_OR_SPARSITY_CANONICALIZATION_ONLY`;
- score nonpositive -> `FF4_GENERIC_CANONICALIZATION_NOT_REJECTED`.

A generic-control result narrows or strengthens the interpretation of the FF4 compiler branch. It cannot erase a separately passed balanced Q1 generator-method contrast and cannot be used to claim that typed FF4 diagnosis explains the Winner-side component of `G_MAIN,A`.

## 11. Rejected-source VALIDATION — Q4 remains independent

Independently evaluate the frozen rejected-source conditions using `S_C` and the 2×2 interaction. Failure removes First-Fail-specific promotion and leaves the Search-Projection/Semantic-Transfer line parked. It cannot invalidate Q1, Q2 classification, or Q3 localization.

## 12. Orthogonal decision table

V4 adjudicates a tuple, not one conjunctive PASS bit:

`(Q1_complete_method, Q2_ff4_diagnosis_class, Q3_observed_realization_excess, Q4_rejected_source)`.

### A. Raw generator SCREEN fails

`STOP_COMPLETE_STATE_GENERATION_METHOD_STORY_SCREEN`

VALIDATION remains sealed. No automatic typed-compiler E3 proposal.

### B. Raw generator VALIDATION fails

`STOP_COMPLETE_STATE_GENERATION_METHOD_STORY_VALIDATION`

Do not rescue with B, generic controls, realization probe, rejected-source result, or E3.

### C. Q1 passes; FF4 generic controls do not establish typed diagnosis

Keep the empirically supported balanced complete state-generation method effect. Separately label the FF4 branch as either:

- `FF4_SCOPE_OR_SPARSITY_CANONICALIZATION_ONLY`, or
- `FF4_GENERIC_CANONICALIZATION_NOT_REJECTED`.

Do not claim trajectory-conditioned typed diagnosis for FF4 and do not imply that either Q2 label explains the Winner-side contribution to Q1. Any E3 proposal for the complete method requires separate adjudication.

### D. Q1 passes; FF4 typed-diagnosis controls pass; Q3 observed excess fails

`PASS_COMPLETE_METHOD_WITH_FF4_TYPED_DIAGNOSIS_DROP_REALIZATION_EXCESS_CLAIM`

The balanced complete method effect remains eligible for a separately reviewed E3 proposal. The FF4 branch may support typed-diagnosis substance beyond the stated controls, but realization/variance-centered mechanism language is removed.

### E. Q1 + FF4 typed diagnosis + Q3 observed excess pass; Q4 fails

`PASS_COMPLETE_METHOD_WITH_FF4_DIAGNOSIS_AND_OBSERVED_REALIZATION_EXCESS_DROP_REJECTED_SOURCE_CLAIM`

This is a clean primary-paper outcome if the generator method helps but rejected evidence is not generally superior. Q2 remains an FF4-specific substance result and Q3 remains an observed excess-disagreement result unless the stronger iid/stationary actor model is explicitly invoked. A separate untouched E3 proposal is still required.

### F. Q1 + FF4 typed diagnosis + Q3 observed excess + Q4 pass

`PASS_FULL_BRIDGE_ELIGIBLE_FOR_SEPARATE_E3_PROPOSAL`

The primary state-generation method and rejected-source moderator are eligible for separately authorized confirmation. This does not merge Semantic-Transfer into the primary claim, and Q2 still does not explain the Winner-side component of Q1.

### G. Sensitivity conflicts

`G_MAIN,AB` and `G_F,AB` never change the Q1 bit. A material conflict is reported as `FREE_REALIZATION_SENSITIVITY_WARNING` and narrows expected-realization language; it does not permit best-of-two selection or estimand switching.

## 13. Cost and exactly-once consequences

V4 adds **zero provider cost** relative to V3.

- `G_MAIN,A`, `G_W`, and `G_F,A` use observations already in the primary validation run.
- `G_MAIN,AB` and `G_F,AB` use only the already-preauthorized B realization in addition to those observations.
- `D_X-D_A` uses exactly the already-preauthorized `(A1,A2,B1,B2)` observations.
- universal state-SHA aliasing across all FREE/COMP/generic arms can only reduce duplicate actor calls; removed calls cannot be reallocated.
- both generic controls remain zero-updater deterministic states.

No third FREE synthesis, third actor replicate, second backbone, public benchmark, or E3 call is authorized.

## 14. Current zero-provider implementation checks

V4 freezes machine-checkable helpers before any scientific execution:

- canonical rendered repair-block count is 0/1/2 and does not expose primitive identity;
- scope-matched control receives scores + block cardinality only;
- identical state SHA across **any** arm pair forces exact-zero state-treatment contrast before Q1/Q2/Q4 arithmetic;
- generic-control classification is independent of the raw generator gate and is explicitly FF4-only;
- raw generator gate can pass while FF4 diagnosis-control classification narrows to scope/generic canonicalization;
- primary generator-factor main effect averages Winner and FF4 generator contrasts equally and uses FF4 FREE_A only;
- source-specific `G_W` and `G_F,A` are components, not Q1 gates;
- `G_MAIN,AB` and `G_F,AB` exist only as sensitivity;
- `D_X-D_A` uses commensurate cross/within disagreement;
- the squared-propensity identity is labeled model-based and requires conditionally iid/independent stationary actor realizations;
- identical FREE A/B state SHA forces observed realization-excess contrast to zero;
- six-stream observed realization-excess gate is independent of the complete-method gate.

Current V4-R2 test count is recorded in the bound zero-provider audit after the full adjacent regression suite is run.

## 15. Current scientific authority

This V4-R2 document grants zero authority for:

- provider calls;
- bridge search-pool acquisition;
- updater execution;
- FREE_A or FREE_B synthesis;
- actor evaluation or actor remeasurement;
- SCREEN outcome opening;
- VALIDATION opening;
- E3;
- second backbone;
- public benchmark;
- paper promotion.

It is a prospective review-repair object only.

Before any Bridge Stage A execution, V4-R2 itself must be frozen/content-addressed and independently reviewed. The canonical primary-line resource rule remains in force: **do not execute the parked Semantic-Transfer Stage A while the State-Generator bridge remains unresolved.**
