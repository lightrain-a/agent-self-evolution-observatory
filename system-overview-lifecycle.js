(() => {
  const pick = (zh, en) => language === "zh" ? zh : en;
  const api = window.SYSTEM_OVERVIEW_SECTIONS = window.SYSTEM_OVERVIEW_SECTIONS || {};
  const step = (index, tag, zhTitle, enTitle, zhQuestion, enQuestion, zhArtifact, enArtifact) => `<article class="system-lifecycle-step"><header><span>${String(index).padStart(2,"0")}</span><small>${esc(tag)}</small></header><h4 data-toc="false">${pick(zhTitle,enTitle)}</h4><p>${pick(zhQuestion,enQuestion)}</p><footer><b>${pick("留下什么","Artifact")}</b><span>${pick(zhArtifact,enArtifact)}</span></footer></article>`;

  api.renderLifecycle = function renderLifecycle() {
    return `<section class="system-lifecycle system-section"><h3>${pick("一项研究如何从“能写成论文”走到全量实验","How a research question moves from paper-worthy novelty to full experiments")}</h3><p class="section-intro">${pick("新的主顺序是 paper-first：先证明论文层 novelty，再设计方法，再把论文主张编译成实验计划；局部实验只验证已经冻结的关键假设，不能边跑 pilot 边发明核心方法。局部验证通过并冻结方法后，才进入全量实验。","The primary order is now paper-first: establish paper-level novelty, design the method, compile paper claims into an experiment blueprint, and use local pilots only to test already-frozen load-bearing assumptions. Core methods are not invented while chasing pilot scores. Full experiments begin only after local validation and method freeze.")}</p><div class="system-lifecycle-grid">
      ${step(1,"SCOPE","论文目标与研究边界","Paper target and research boundary","要投什么会议、解决什么问题、论文最终允许声称什么？","What venue, problem, and final claim boundary define the paper?","版本化 paper scope + resource policy","versioned paper scope + resource policy")}
      ${step(2,"EVIDENCE","文献、最近邻与真实缺口","Evidence, closest work, and real gap","最近工作已经做到哪里？我们的缺口是方法缺口、理论缺口还是评测缺口？","What has the closest work already solved, and is the remaining gap methodological, theoretical, or evaluative?","closest-work table + provenance + evidence/collision graph","closest-work table + provenance + evidence/collision graph")}
      ${step(3,"NOVELTY","论文 Novelty 合同","Paper Novelty Contract","如果现在写 Introduction 和 Contributions，核心新意能否一句话说清，并且不是最强近邻/简单 baseline 的改写？","If we wrote the Introduction and Contributions now, is the central novelty explicit and irreducible to the closest work or a simple baseline?","Paper Novelty Contract：gap + closest work + novelty axis + contribution claim + irreducible difference","Paper Novelty Contract: gap + closest work + novelty axis + contribution claim + irreducible difference")}
      ${step(4,"METHOD","原理与方法设计","Principle and method design","什么机制解决这个 gap？方法的每个 load-bearing component 如何对应 novelty，而不是为了实验效果临时添加？","What mechanism solves the gap, and how does each load-bearing component implement the novelty rather than patching a pilot score?","Principle Certificate + Method Contract + novelty→method mapping + strongest simplification","Principle Certificate + Method Contract + novelty→method mapping + strongest simplification")}
      ${step(5,"EXPERIMENT PLAN","论文主张→实验蓝图","Paper claims → experiment blueprint","每条 contribution 需要哪张主表、哪个 baseline、哪组消融、什么机制实验才能支撑？局部验证和全量实验分别负责什么？","Which main table, baseline, ablation, and mechanism test supports each contribution? What belongs to local validation versus the full experiment?","Claim→Experiment matrix + baseline/ablation matrix + local/full split + freeze rule","Claim→Experiment matrix + baseline/ablation matrix + local/full split + freeze rule")}
      ${step(6,"ECONOMY+COMPILE","资源经济与实验编译","Economy and experiment compile","完整实验计划里，最便宜的决定性验证是什么？Protocol、Updater、统计与算力条件是否允许局部实验启动？","What is the cheapest decisive test in the full blueprint, and do protocol, updater, statistics, and compute conditions permit local validation?","Economy 5/5 + Protocol Validity + REP + Pre-Experiment Card + 8 Gate","Economy 5/5 + Protocol Validity + REP + Pre-Experiment Card + eight gates")}
      ${step(7,"LOCAL VALIDATION","局部实验验证","Local experimental validation","只用最小样本/单模型/少量 seed，能否验证 phenomenon、可辨识性、机制方向和最强 baseline 分歧？","With minimal cases/models/seeds, can we validate the phenomenon, identifiability, mechanism direction, and disagreement with the strongest baseline?","F0 + P0-Support + minimal P0-Method evidence；只回答预注册风险","F0 + P0-Support + minimal P0-Method evidence; only preregistered risks are answered")}
      ${step(8,"METHOD FREEZE","方法冻结与论文级复审","Method freeze and paper-level re-review","局部结果是否支持原设计？如果核心方法需要改变，新的方法是否仍然有同样/更强 novelty？","Do local results support the designed method? If the core method must change, does the revised method still have a defensible paper-level novelty?","method hash + blueprint hash + local-validation decision；核心改动则返回 NOVELTY/METHOD","method hash + blueprint hash + local-validation decision; core changes return to NOVELTY/METHOD")}
      ${step(9,"FULL EXPERIMENT","全量实验","Full experiment","冻结方法后，主表、跨 seed/backbone/domain、强 baseline、消融、效率和泛化能否一次性按蓝图收齐？","After method freeze, can main tables, cross-seed/backbone/domain results, strong baselines, ablations, efficiency, and generalization be collected under the frozen blueprint?","P1/full-scale result package + replication + ablations + efficiency","P1/full-scale result package + replication + ablations + efficiency")}
      ${step(10,"PAPER EVIDENCE","论文证据闭环","Paper evidence closure","每条 novelty/contribution claim 是否都有对应实验 artifact、引用和实现证据，且没有超出结果说话？","Does every novelty/contribution claim have matching experiment, citation, and implementation evidence without overclaiming?","Evidence Integrity / Chain-of-Evidence + paper-ready tables/figures","Evidence Integrity / Chain-of-Evidence + paper-ready tables/figures")}
      ${step(11,"LEARN","经验沉淀与系统更新","Learning and system update","这次 novelty、方法设计、实验计划和失败诊断经验如何变成下一轮的规则？","How do novelty, method-design, experiment-planning, and failure lessons become rules for the next project?","规则/测试/配置/Failure Asset 更新 + 公开快照","rule/test/config/Failure Asset updates + public snapshot")}
    </div><div class="system-retro-note">${pick("最重要的新约束：局部实验不能负责“找方法”。Pilot 只验证 Novelty/Method/Experiment Blueprint 已经预注册的关键风险；一旦核心方法因 pilot 而改变，原 full-experiment authorization 自动失效，必须回到 NOVELTY → METHOD → EXPERIMENT PLAN 重新审查。","Critical rule: local experiments do not discover the method. Pilots only test risks preregistered by the Novelty/Method/Experiment Blueprint. If a pilot changes the core method, all full-experiment authorization is invalidated and the project returns to NOVELTY → METHOD → EXPERIMENT PLAN.")}</div></section>
    <section class="system-contract-panel system-section"><h3>${pick("Paper-first 数据合同：每一层凭什么继续","Paper-first stage contracts: what authorizes continuation")}</h3><div class="history-table-scroll"><table class="matrix system-contract-table"><thead><tr><th>${pick("阶段","Stage")}</th><th>${pick("必须回答","Must answer")}</th><th>${pick("允许继续的条件","Continuation condition")}</th><th>${pick("失败时怎么办","Failure behavior")}</th></tr></thead><tbody>
      <tr><th>NOVELTY</th><td>${pick("论文为什么值得发表？和 closest work 的实质差异是什么？","Why is the paper publishable, and what is materially different from the closest work?")}</td><td>${pick("gap、closest work、novelty axis、contribution claim、irreducible difference 全部冻结。","Gap, closest work, novelty axis, contribution claim, and irreducible difference are frozen.")}</td><td>${pick("回到问题/文献层重构，不进入实现。","Reframe the problem/evidence; do not implement.")}</td></tr>
      <tr><th>METHOD</th><td>${pick("方法如何实现 novelty？每个组件是否 load-bearing？","How does the method instantiate the novelty, and is each component load-bearing?")}</td><td>${pick("Principle + Method Contract 完整，且能指出 strongest simplification。","Principle + Method Contract is complete and the strongest simplification is explicit.")}</td><td>${pick("修改方法设计，而不是先跑实验再靠结果补方法。","Revise method design instead of running experiments and patching the method afterward.")}</td></tr>
      <tr><th>EXPERIMENT PLAN</th><td>${pick("每条论文 claim 如何被实验支撑或证伪？","How is each paper claim supported or falsified experimentally?")}</td><td>${pick("Claim→Experiment、baseline、ablation、local/full split 与 freeze rule 完整。","Claim→Experiment, baselines, ablations, local/full split, and freeze rule are complete.")}</td><td>${pick("补实验逻辑；不允许无计划地启动 GPU。","Repair the experimental logic; no unplanned GPU launch.")}</td></tr>
      <tr><th>LOCAL VALIDATION</th><td>${pick("最关键假设在最小实验上成立吗？","Do the load-bearing assumptions survive the cheapest local tests?")}</td><td>${pick("F0/P0 只验证预注册机制、现象、可辨识性和 baseline 分歧。","F0/P0 validates only preregistered mechanism, phenomenon, identifiability, and baseline disagreement.")}</td><td>${pick("设计/测量问题就修对应层；核心方法若改变，返回 NOVELTY/METHOD。","Repair the affected design/measurement layer; a core method change returns to NOVELTY/METHOD.")}</td></tr>
      <tr><th>METHOD FREEZE</th><td>${pick("局部结果是否足以冻结方法进入全量实验？","Do local results justify freezing the method for full experiments?")}</td><td>${pick("method hash + experiment-blueprint hash 冻结，核心 contribution 不再随实验漂移。","Method hash + experiment-blueprint hash are frozen; the core contribution no longer drifts with experiments.")}</td><td>${pick("不冻结就不扩大；禁止“边做全量边继续设计核心方法”。","No freeze, no scale-up; core method design cannot continue during full experiments.")}</td></tr>
      <tr><th>FULL EXPERIMENT</th><td>${pick("冻结方法是否在完整论文证据矩阵上成立？","Does the frozen method survive the complete paper evidence matrix?")}</td><td>${pick("按预定主表、baseline、消融、replication、efficiency/generality 一次性执行。","Execute the preregistered main tables, baselines, ablations, replication, efficiency, and generalization matrix.")}</td><td>${pick("结果用于收缩 claim 或触发新的研究迭代，不允许静默改核心方法继续混跑。","Use results to narrow claims or start a new research iteration; never silently mutate the core method mid-run.")}</td></tr>
      <tr><th>PAPER EVIDENCE</th><td>${pick("论文文字与真实证据完全对齐吗？","Does paper wording match the actual evidence?")}</td><td>${pick("Chain-of-Evidence 无 unresolved high-risk gap。","Chain-of-Evidence has no unresolved high-risk gap.")}</td><td>${pick("修 claim、补合法证据或降低结论，不用语言包装掩盖证据缺口。","Repair the claim, add valid evidence, or narrow the conclusion; never hide an evidence gap with wording.")}</td></tr>
    </tbody></table></div></section>`;
  };
})();
