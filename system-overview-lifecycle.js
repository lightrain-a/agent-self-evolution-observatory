(() => {
  const pick = (zh, en) => language === "zh" ? zh : en;
  const api = window.SYSTEM_OVERVIEW_SECTIONS = window.SYSTEM_OVERVIEW_SECTIONS || {};
  const step = (index, tag, zhTitle, enTitle, zhQuestion, enQuestion, zhArtifact, enArtifact) => `<article class="system-lifecycle-step"><header><span>${String(index).padStart(2,"0")}</span><small>${esc(tag)}</small></header><h4 data-toc="false">${pick(zhTitle,enTitle)}</h4><p>${pick(zhQuestion,enQuestion)}</p><footer><b>${pick("留下什么","Artifact")}</b><span>${pick(zhArtifact,enArtifact)}</span></footer></article>`;

  api.renderLifecycle = function renderLifecycle() {
    return `<section class="system-lifecycle system-section"><h3>${pick("一项研究如何流过系统","How one research question moves through the system")}</h3><p class="section-intro">${pick("现在按“决策问题”组织流程，而不是按脚本或文件名组织。每一层回答一个问题，并留下授权下一层继续的结构化工件。","The flow is organized by research decisions rather than scripts. Each stage answers one question and leaves a structured artifact that authorizes the next stage.")}</p><div class="system-lifecycle-grid">
      ${step(1,"SCOPE","研究合同","Research contract","研究问题、会议、截止日期、资源上限和主张边界是什么？","What are the research question, venue, cutoff, resource ceiling, and claim boundary?","版本化 scope + resource policy","versioned scope + resource policy")}
      ${step(2,"EVIDENCE","文献与证据","Evidence and prior work","已有工作做到哪里，最近碰撞在哪里，哪些事实仍未知？","What has prior work established, where are the closest collisions, and what remains unknown?","去重语料 + provenance + evidence graph","deduplicated corpus + provenance + evidence graph")}
      ${step(3,"HYPOTHESIS","问题与可证伪假设","Problem and falsifiable hypothesis","真正要解释的 failure 是什么？什么观察会支持或推翻机制？","What failure needs explanation, and what observation would support or falsify the mechanism?","problem capsule + falsifier + matched baseline","problem capsule + falsifier + matched baseline")}
      ${step(4,"ECONOMY","P0 经济门","P0 Economy Gate","先判断实验值不值得跑：简化基线、底座库存、因果单位、VOI 与 single-writer authority 是否全部成立？","First decide whether the run is worth doing: do simplification headroom, substrate inventory, causal unit, VOI, and single-writer authority all hold?","Economy 5/5 card + simplification tournament + substrate inventory + VOI + authority lease","Economy 5/5 card + simplification tournament + substrate inventory + VOI + authority lease")}
      ${step(5,"COMPILE","实验设计编译","Experiment compile","Economy 通过后，Updater 会产生有效更新动作吗？随后 8 个正式 Gate 是否全部通过？","After Economy passes, can the updater produce meaningful update actions, and do all eight formal gates pass?","Updater Competence prerequisite + Pre-Experiment Card + 8 个正式 Gate；Gate 3 再过 10 项子审计","Updater Competence prerequisite + Pre-Experiment Card + eight formal gates, with a ten-check Gate-3 sub-audit")}
      ${step(6,"SUBSTRATE+F0","底座资格与离线可辨识性","Substrate qualification + F0","更新表面能否产生足够真实效应？现有 trace 上 cheapest falsifier、oracle ceiling、target variation 和 simple baseline 是否先过关？","Can the update surface produce enough real effects, and do the cheapest falsifier, oracle ceiling, target variation, and simple baselines clear F0 first?","substrate card + CPU F0 + oracle/baseline ceiling","substrate card + CPU F0 + oracle/baseline ceiling")}
      ${step(7,"P0-SUPPORT","P0-S · 现象支持","P0-S · Phenomenon support","独立真值与 fresh replication 下，benefit/harm/effect coverage 是否稳定到足以研究方法？","Under independent truth and fresh replication, are benefit/harm/effect coverage stable enough to study a method?","frozen support decision + replicated effect table","frozen support decision + replicated effect table")}
      ${step(8,"P0-METHOD","P0-M · 方法实验","P0-M · Method experiment","只有 Support PASS 后，复杂方法是否优于最强 matched simplification？","Only after Support PASS, does the method beat the strongest matched simplification?","method table + typed METHOD-PASS/FAIL/INCONCLUSIVE","method table + typed METHOD-PASS/FAIL/INCONCLUSIVE")}
      ${step(9,"P1","跨 seed / backbone 复现","Cross-seed / backbone replication","方法结论是否在冻结协议下跨 seed / backbone 保持方向与效应？","Does the method result preserve direction and effect under frozen cross-seed/backbone replication?","P1 replication package + CI","P1 replication package + CI")}
      ${step(10,"PAPER","论文实验","Paper experiments","主表、消融、机制证据、效率和外部泛化是否在不改变核心协议的前提下补齐？","Can main tables, ablations, mechanism evidence, efficiency, and external generalization be completed without changing the core protocol?","paper-ready evidence package","paper-ready evidence package")}
      ${step(11,"LEARN","经验沉淀与发布","Learning and publication","这次踩到的坑如何变成下一轮启动前的规则，而不是只留在聊天记录里？","How does the run become a launch rule for future work rather than a chat-memory anecdote?","规则/测试/配置更新 + 公开快照","rule/test/config update + public snapshot")}
    </div><div class="system-retro-note">${pick("AI 会诊是横向诊断层：插入 HYPOTHESIS、ECONOMY、首次昂贵 LAUNCH、弱结果诊断、以及 SCALE-UP/第二 backbone 前；它不替代 7-stage scientific authority，也不能自行解锁下一阶段。","AI consultation is a cross-cutting diagnostic overlay at HYPOTHESIS, ECONOMY, the first expensive LAUNCH, weak-result diagnosis, and before SCALE-UP/second backbone. It does not replace the seven-stage scientific authority or unlock a stage by itself.")}</div></section>
    <section class="system-contract-panel system-section"><h3>${pick("系统的数据合同：每一层凭什么继续","Stage contracts: what authorizes the next stage")}</h3><div class="history-table-scroll"><table class="matrix system-contract-table"><thead><tr><th>${pick("阶段","Stage")}</th><th>${pick("必须回答","Must answer")}</th><th>${pick("允许继续的条件","Continuation condition")}</th><th>${pick("失败时怎么办","Failure behavior")}</th></tr></thead><tbody>
      <tr><th>SCOPE</th><td>${pick("目标和边界是否明确？","Are goal and boundaries explicit?")}</td><td>${pick("有版本化 scope、资源上限、截止日期。","Versioned scope, resource cap, cutoff.")}</td><td>${pick("不启动检索或实验。","Do not start retrieval or experiments.")}</td></tr>
      <tr><th>EVIDENCE</th><td>${pick("最近工作与未知项是否核清？","Are nearest work and unknowns resolved?")}</td><td>${pick("关键主张有 primary-source provenance。","Critical claims have primary-source provenance.")}</td><td>${pick("继续补检索；标题相似不能代替机制审查。","Retrieve more evidence; title similarity never substitutes for mechanism review.")}</td></tr>
      <tr><th>ECONOMY</th><td>${pick("是否有 simple-baseline headroom、合格 substrate、正确 causal unit、decision-changing VOI 与 single-writer authority？","Are there simple-baseline headroom, a qualified substrate, a correct causal unit, decision-changing VOI, and single-writer authority?")}</td><td>${pick("Economy 5/5 PASS 后才允许进入实验编译；它不是第九个 Pre-Experiment Gate。","Only Economy 5/5 may enter experiment compilation; this is not a ninth Pre-Experiment gate.")}</td><td>${pick("FAIL 就在 GPU 前 simplify / merge / 换 substrate / 修 formulation；第二 backbone 不得救场。","On failure, simplify/merge/change substrate/repair formulation before GPU; a second backbone may not rescue it.")}</td></tr>
      <tr><th>COMPILE</th><td>${pick("Updater/action space 是否先通过 competence prerequisite？实验随后是否可辨识、统计上可分、资源上可完成？","Does the updater/action space first pass the competence prerequisite, and is the experiment then identifiable, statistically resolvable, and computationally feasible?")}</td><td>${pick("Updater Competence prerequisite PASS，并且正式 8 Gate 全 PASS；Updater 不算第九门。","Updater Competence prerequisite passes and all eight formal gates pass; updater competence is not a ninth gate.")}</td><td>${pick("Updater 不合格就停止当前 substrate/action stream；其他 Gate 失败则修对应实验层，不允许“先跑起来再说”。","If updater competence fails, stop the current substrate/action stream; otherwise repair the failed experiment layer—no launch-first-debug-later shortcut.")}</td></tr>
      <tr><th>SUBSTRATE / F0</th><td>${pick("底座能否产生真实效应？CPU falsifier / oracle ceiling / target variation 是否先过？","Can the substrate create real effects, and do CPU falsifiers/oracle ceiling/target variation pass first?")}</td><td>${pick("competence、effect support、representability 与 cheapest falsifier 全部达到冻结下限。","Competence, effect support, representability, and the cheapest falsifier clear frozen floors.")}</td><td>${pick("换 substrate 或一次原子修复；不允许进入方法训练。","Change substrate or make one atomic repair; method training stays blocked.")}</td></tr>
      <tr><th>P0-SUPPORT</th><td>${pick("fresh replication 下 benefit/harm/effect coverage 是否稳定？","Are benefit/harm/effect coverage stable under fresh replication?")}</td><td>${pick("冻结 Support Gate PASS，才产生 method authorization。","A frozen Support Gate PASS is required to authorize method work.")}</td><td>${pick("SUPPORT_INSUFFICIENT / HOLD；绝不写 METHOD-FAIL。","SUPPORT_INSUFFICIENT / HOLD; never METHOD-FAIL.")}</td></tr>
      <tr><th>P0-METHOD</th><td>${pick("方法是否优于最强 matched simplification？","Does the method beat the strongest matched simplification?")}</td><td>${pick("读取冻结 Support PASS；独立真值、matched budget、baseline 与统计门全部满足。","Read a frozen Support PASS; independent truth, matched budget, baselines, and statistical gates all hold.")}</td><td>${pick("只有这一层才允许正式 METHOD-FAIL / METHOD-PASS / INCONCLUSIVE。","Only this layer may emit METHOD-FAIL / METHOD-PASS / INCONCLUSIVE.")}</td></tr>
      <tr><th>LEARN</th><td>${pick("这次经验有没有变成系统约束？","Did the lesson become a system constraint?")}</td><td>${pick("至少落成规则、测试、配置或监控项之一。","At least one rule, test, config, or monitoring check is added.")}</td><td>${pick("不把一次性人工记忆当作已解决。","Do not treat human memory as a durable fix.")}</td></tr>
    </tbody></table></div></section>`;
  };
})();
