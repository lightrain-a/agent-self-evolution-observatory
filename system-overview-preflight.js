(() => {
  const pick = (zh, en) => language === "zh" ? zh : en;
  const api = window.SYSTEM_OVERVIEW_SECTIONS = window.SYSTEM_OVERVIEW_SECTIONS || {};
  const gate = (index, tag, zhTitle, enTitle, zhQuestion, enQuestion, zhStop, enStop, zhArtifact, enArtifact) => `<article class="preflight-gate"><header><span>${String(index).padStart(2,"0")}</span><small>${esc(tag)}</small></header><h4 data-toc="false">${pick(zhTitle,enTitle)}</h4><p>${pick(zhQuestion,enQuestion)}</p><dl><div><dt>${pick("不通过就","If it fails")}</dt><dd>${pick(zhStop,enStop)}</dd></div><div><dt>${pick("启动前工件","Required artifact")}</dt><dd>${pick(zhArtifact,enArtifact)}</dd></div></dl></article>`;
  const lesson = (tone, tag, zhTitle, enTitle, zhObserved, enObserved, zhCause, enCause, zhRule, enRule) => `<article class="system-lesson ${tone}"><header><span>${esc(tag)}</span><h4 data-toc="false">${pick(zhTitle,enTitle)}</h4></header><dl><div><dt>${pick("这次看到","Observed")}</dt><dd>${pick(zhObserved,enObserved)}</dd></div><div><dt>${pick("根因","Root cause")}</dt><dd>${pick(zhCause,enCause)}</dd></div><div><dt>${pick("沉淀规则","Rule added")}</dt><dd>${pick(zhRule,enRule)}</dd></div></dl></article>`;

  function renderCompiler() {
    return `<section class="preflight-compiler system-section"><div class="preflight-heading"><div><span class="system-kicker">PRE-EXPERIMENT COMPILER</span><h3>${pick("第一次 GPU 调用之前，必须先过 8 个 Gate","Eight gates before the first GPU call")}</h3><p>${pick("这次最大的经验是：过去只检查“方向是否合理、代码能不能跑”，缺少“实验在什么条件下才有资格给科学结论”的审查层。现在把事后排障链反转成启动前编译。","The missing layer was scientific executability: under what conditions is a run entitled to produce a scientific conclusion? Post-hoc debugging is now inverted into a pre-launch compiler.")}</p></div><div class="preflight-lock"><b>LAUNCH</b><span>${pick("8/8 PASS 才解锁","unlocked only at 8/8 PASS")}</span></div></div><div class="preflight-gate-grid">
      ${gate(1,"PROVENANCE","Literature-to-Protocol","Literature-to-Protocol","每个关键参数为什么是这个值？","Why does each critical parameter have this value?","补文献、统计计算或明确 protocol choice。","Add literature, statistical derivation, or an explicit protocol choice.","parameter provenance table","parameter provenance table")}
      ${gate(2,"COMPETENCE","Baseline Competence","Baseline competence","没有我们的方法，Base agent 本身会不会任务？","Can the base agent solve the task without our method?","换 scaffold/backbone/任务设置；不能判方法失败。","Change scaffold, backbone, or task setting; never call this a method failure.","competence qualification report","competence qualification report")}
      ${gate(3,"IDENTIFY","Mechanism Identifiability","Mechanism identifiability","方法真和方法假时，当前设计会得到不同数据吗？","Would the design produce distinguishable data when the mechanism is true versus false?","先做 synthetic counterfactual / floor-ceiling 检查。","Run synthetic counterfactual and floor/ceiling checks first.","identifiability simulation","identifiability simulation")}
      ${gate(4,"STATS","Statistical Resolution","Statistical resolution","1/n 的离散步长、预期 prevalence 和 CI 能支持阈值吗？","Can 1/n resolution, expected prevalence, and CI support the threshold?","增加样本或改变 metric；不能用过细阈值配过少样本。","Increase samples or change the metric; do not pair tiny thresholds with coarse data.","resolution / power / CI worksheet","resolution / power / CI worksheet")}
      ${gate(5,"COMPUTE","Compute Graph","Compute graph","nested loops 展开后到底是多少 episodes、steps、generations、tokens？","After expanding nested loops, how many episodes, steps, generations, and tokens are there?","缩减无关维度、分阶段，或改 inference path；不是盲砍科学样本。","Remove irrelevant dimensions, stage the experiment, or optimize inference—not scientific evidence away.","expected + worst-case compute graph","expected + worst-case compute graph")}
      ${gate(6,"THROUGHPUT","Measured Throughput","Measured throughput","这台机器上完整代表性 episode 的 calls/hour 是多少？","What is the measured calls/hour for representative full episodes on this machine?","先跑 3–5 个完整代表 episode，再根据实测吞吐定 GPUh。","Run 3–5 representative full episodes before deriving GPU-hour caps.","machine-specific throughput calibration","machine-specific throughput calibration")}
      ${gate(7,"RECOVERY","Observability & Recovery","Observability & recovery","中途死掉时能留下什么？现在跑到第几个 candidate 能不能知道？","What survives a crash, and can we tell which candidate is running?","补 incremental trace、progress、heartbeat、resume、在线 watchdog。","Add incremental traces, progress, heartbeat, resume, and online watchdog.","checkpoint/recovery plan","checkpoint/recovery plan")}
      ${gate(8,"SEMANTICS","Outcome Semantics","Outcome semantics","什么情况叫 METHOD-FAIL，什么情况只能叫 INCONCLUSIVE？","What is METHOD-FAIL and what can only be INCONCLUSIVE?","预注册 typed outcomes；无法归类就不允许 launch。","Preregister typed outcomes; if the run cannot be classified, do not launch.","outcome taxonomy + Go/Stop rules","outcome taxonomy + Go/Stop rules")}
    </div></section>
    <section class="preflight-card system-section"><h3>${pick("一个合格的 Pre-Experiment Card 应该长什么样","What a launch-ready Pre-Experiment Card contains")}</h3><div class="preflight-card-grid"><div><b>${pick("科学变量","Scientific variable")}</b><span>${pick("唯一改变什么；最强简化 Baseline 是谁；独立真值来自哪里。","What changes, the strongest simplification baseline, and the independent truth source.")}</span></div><div><b>${pick("参数来源","Parameter provenance")}</b><span>literature / statistical calculation / measured throughput / explicit protocol choice</span></div><div><b>${pick("可辨识区间","Identifiable regime")}</b><span>${pick("Base success floor/ceiling、最少 harmful/positive cases、任务类型覆盖。","Base success floor/ceiling, minimum harmful/positive cases, task-family coverage.")}</span></div><div><b>${pick("统计分辨率","Statistical resolution")}</b><span>${pick("最小可观测变化、预计 CI、screening 与 confirmatory 的不同权限。","Minimum observable change, expected CI, and distinct screening/confirmatory authority.")}</span></div><div><b>${pick("计算图","Compute graph")}</b><span>${pick("episode → step → generation → token → expected/worst GPUh。","episode → step → generation → token → expected/worst GPUh.")}</span></div><div><b>${pick("恢复与审计","Recovery & audit")}</b><span>${pick("run dir、GPU UUID、incremental artifacts、heartbeat、budget stop、resume。","run dir, GPU UUID, incremental artifacts, heartbeat, budget stop, resume.")}</span></div></div></section>`;
  }

  function renderLessons() {
    return `<section class="system-lessons system-section"><h3>${pick("把事后排障变成事前规则","Turn post-hoc debugging into pre-launch rules")}</h3><p class="section-intro">${pick("这些卡片记录科研系统本身学到了什么。后续任何方向都会复用这些约束，而不是只在聊天记录里记住一次。","These cards record what the research system learned. Every future project inherits the rules instead of relying on chat memory.")}</p><div class="system-lesson-grid">
      ${lesson("compute","COMPUTE","“≤500 episodes”并不等于小实验","An episode cap can hide a huge compute graph","旧设计最坏 472 episodes × 50 steps，约 23,600 次动作 generation。","The old plan allowed 472 episodes × 50 steps—about 23,600 action generations.","只在 episode 层估算资源，没有展开 step/generation。","Resource estimation stopped at episodes instead of expanding steps and generations.","启动前必须输出 expected/worst compute graph，并用实测 calls/hour 定 GPUh。","Every launch must expand the compute graph and derive GPU-hour caps from measured calls/hour.")}
      ${lesson("floor","FLOOR","366/366 全失败不能叫方法 FAIL","366/366 failed rollouts are not a method failure","A-2 旧 run 所有环境 success=0，却一度被登记成 controller FAIL。","An earlier A-2 run had zero success on all 366 episodes but was initially registered as a controller failure.","Base agent 没进入“保持成功同时省调用”的可测区间。","The base agent never entered the regime where success-preserving call savings were measurable.","增加 competence + identifiability gate；旧结果保留审计但科学作废。","Add competence and identifiability gates; preserve the artifact for audit but invalidate it scientifically.")}
      ${lesson("scaffold","AGENT","模型加载成功不等于 Agent 合格","A loaded model is not a qualified agent","generic react-lite 在 Qwen/Llama 的 24 个 ID 环境都是 0 success；修复后 Qwen 为 8/24，OOD 为 41/134。","Generic react-lite produced zero success for both Qwen and Llama over 24 ID environments; after repair Qwen reached 8/24 ID and 41/134 OOD.","任务目标在第 0 步后丢失，scaffold 也缺少 task-family few-shot 结构。","The task goal disappeared after step zero and the scaffold lacked task-family few-shot structure.","每步保留 goal；按任务类型 few-shot；24-case calibration 后再做 134-case qualification。","Keep the goal at every step, use task-family few-shot scaffolds, and require 24-case calibration before 134-case qualification.")}
      ${lesson("stats","STATS","小样本会把 1 个 flip 放大成结论","Small samples turn one flip into a conclusion","8 candidates、少量 hidden task 时，一个 binary flip 就能改变十几个百分点。","With eight candidates and few hidden tasks, one binary flip moves the estimate by double-digit percentage points.","启动前没有计算 1/n 分辨率、harmful prevalence 和 CI。","We had not computed 1/n resolution, harmful prevalence, or expected confidence intervals.","screening 不产生正式 FAIL；confirmatory 增样本并使用 95% bootstrap CI，跨 0 视为 inconclusive。","Screening cannot issue a formal FAIL; confirmatory runs use larger samples and 95% bootstrap CIs, with zero-crossing treated as inconclusive.")}
      ${lesson("persistence","RECOVERY","一次性写盘让“在跑”看起来像“卡死”","End-only persistence makes active work look stuck","旧 A-1 数小时没有 candidate/trace 文件，进程死掉也无法复用已完成工作。","The old A-1 produced no candidate/trace artifacts for hours and would lose completed work on failure.","records 只在 collection 结束时统一写。","Records were accumulated in memory and written only at the end.","每 episode append trace、每 candidate/sequence 立即写、progress/state 原子更新、在线预算检查。","Append traces per episode, persist each candidate/sequence immediately, atomically refresh progress/state, and enforce budgets online.")}
      ${lesson("scheduler","SCHEDULER","GPU index 不是可靠的物理卡身份","GPU index is not a reliable physical identity","在 60 上 CUDA_VISIBLE_DEVICES=1 一度落到 nvidia-smi 的物理 GPU0。","On server 60, CUDA_VISIBLE_DEVICES=1 initially mapped to physical GPU0 in nvidia-smi.","CUDA ordinal 与 nvidia-smi index 顺序不一致。","CUDA ordinal ordering differed from nvidia-smi ordering.","调度器改为 GPU UUID 绑定；per-run state/lock，允许跨 GPU/服务器并行。","The scheduler now binds GPU UUIDs and isolates per-run states/locks so distinct experiments can run in parallel.")}
    </div></section>
    <section class="system-semantics system-section"><h3>${pick("实验结果必须先分类，再解释","Classify the run before interpreting it")}</h3><div class="system-semantics-grid"><article class="method"><b>METHOD-PASS / METHOD-FAIL</b><p>${pick("只有 competence、identifiability、预算、对照、独立真值和统计门全部满足时才允许。","Allowed only when competence, identifiability, budget, controls, independent truth, and statistical gates all hold.")}</p></article><article><b>SCREENING-SIGNAL / NO-SIGNAL</b><p>${pick("只决定是否值得扩样；no-signal 不能自动杀掉方向。","Controls whether evidence should be expanded; no-signal does not automatically kill a direction.")}</p></article><article><b>INCONCLUSIVE</b><p>${pick("CI 太宽、positive/harmful cases 太少或 outcome variation 不够。","CI too wide, too few positive/harmful cases, or insufficient outcome variation.")}</p></article><article><b>BASELINE-FLOOR / CEILING</b><p>${pick("Base agent 太弱或太强，方法变量没有可测空间。","The base agent is too weak or too strong for the method variable to be measurable.")}</p></article><article><b>RUNTIME / IMPLEMENTATION-ERROR</b><p>${pick("环境、代码、路径、模型加载或 parser 的问题；没有方法级结论。","Environment, code, path, model loading, or parser failure; no method-level conclusion.")}</p></article><article><b>BUDGET-STOP</b><p>${pick("watchdog 达到冻结上限；保留 partial artifacts，但不得注册科学结果。","The watchdog reached the frozen cap; keep partial artifacts but do not register a scientific result.")}</p></article></div></section>`;
  }

  api.renderPreflight = () => `${renderCompiler()}${renderLessons()}`;
})();
