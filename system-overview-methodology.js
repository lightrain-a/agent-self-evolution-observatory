(() => {
  const pick=(zh,en)=>language==="zh"?zh:en;
  const api=window.SYSTEM_OVERVIEW_SECTIONS=window.SYSTEM_OVERVIEW_SECTIONS||{};
  const labels={
    "exploration-frontier":["候选问题是否过于扎堆","Are candidate problems too similar?","除了检查单个 Idea 是否已经被论文做过，还要检查整批候选是不是都在同一批种子论文附近打转。如果十个候选只是同一个问题的轻微改写，就重新分配检索方向；这不代表其中某一篇论文自动失败。","Besides checking whether one idea is already covered, check whether the whole batch stays near the same seed papers. If ten candidates are only small rewrites of one question, redirect the search; this does not automatically reject any individual paper."],
    "experimental-design-integrity":["结果出来前固定实验设置，并检查数据泄漏","Freeze the setup before results and check leakage","在看测试结果之前先固定模型、Prompt/工具、训练/测试划分、指标、重复次数、分析方法、停止规则和排除规则。联网 Agent 还要检查搜索结果里是否暴露了基准题目、答案或能直接推断答案的元数据。看到结果后再改这些设置，就必须登记成新的实验版本。","Before seeing test outcomes, fix the model, prompt/tools, train/test split, metrics, replication count, analysis, stopping rules, and exclusions. Web-enabled agents must also check whether search results expose benchmark questions, answers, or metadata that reveals answers. Any redesign after seeing outcomes becomes a new experiment version."],
    "reproducibility-readiness":["别人能否从零重跑关键结果","Can another person rerun the key result from scratch?","“论文数字能找到对应文件”还不等于可复现。关键结果还要提供依赖版本、环境配置、数据来源、运行命令和输入输出关系，并让独立执行者真正重新计算，而不是直接读取仓库里已经保存的结果文件。","Being able to trace a paper number to a file is not enough. Key results must include dependency versions, environment configuration, data sources, commands, and input/output relationships, and an independent runner must recompute them rather than read checked-in result files."],
    "mechanism-to-intervention-closure":["机制分析有没有形成科学闭环","Does the mechanism analysis lead to a scientific closure?","发现现象、排除简单解释并定位瓶颈以后，再问一次：这个机制是否自然导出一个可以事先冻结、能够被最强同信息简单基线反驳的干预？如果答案是“有”，就必须把它变成新的 Go/Stop 实验；如果答案是“没有”，则明确把论文定位成 measurement / identification / negative-boundary paper，并说明它具体改变了什么评测规则或工程决策。禁止为了看起来像顶会论文而人为堆算法、验证器或公式。","After finding a phenomenon, ruling out simple explanations, and localizing a bottleneck, ask whether the mechanism naturally implies a preregistrable intervention that can be falsified by the strongest same-information baseline. If yes, turn it into a new Go/Stop experiment. If not, explicitly choose a measurement / identification / negative-boundary archetype and state which evaluation or engineering decision changes. Never add algorithms, validators, or equations merely to look more sophisticated."],
  };
  api.renderMethodologyControls=function renderMethodologyControls(state){
    const method=state.methodology_controls||{},summary=method.summary||{},controls=method.controls||[];
    const cards=controls.map((row,index)=>{const l=labels[row.key]||[row.key,row.key,"",""];return `<article class="methodology-control-card"><header><span>${String(index+1).padStart(2,"0")}</span><small>${esc(row.primary_layer||"")}</small></header><h4 data-toc="false">${pick(l[0],l[1])}</h4><p>${pick(l[2],l[3])}</p><dl><div><dt>${pick("Owner","Owner")}</dt><dd><code>${esc(row.owner_component||"--")}</code></dd></div><div><dt>${pick("当前状态","Status")}</dt><dd>${esc(row.status||"--")}</dd></div><div><dt>${pick("参考设计","Design sources")}</dt><dd>${esc((row.design_sources||[]).join(" · "))}</dd></div></dl></article>`}).join("");
    return `<section class="system-methodology system-section"><div class="preflight-heading"><div><span class="system-kicker">CROSS-CUTTING METHODOLOGY CONTROLS</span><h3>${pick("再检查四个最容易被忽略、但会直接影响论文可信度的问题","Four extra checks that directly affect whether a paper is trustworthy")}</h3><p>${pick("第一，候选问题不能全部挤在同一批论文附近；第二，实验设置要在结果出来前固定并检查联网数据泄漏；第三，关键结果要能由独立执行者从零重跑；第四，机制分析在定位瓶颈后必须判断是否自然导出可证伪干预，或者诚实停在 measurement / identification / negative-boundary archetype。复杂算法本身不是闭环。这四项只是现有论文设计、实验设计和证据检查的一部分，不会自己决定论文通过或给实验授权。","First, candidate problems should not all cluster around the same seed papers. Second, experiment settings must be fixed before outcomes and web-data leakage must be checked. Third, key results must be independently rerunnable from scratch. Fourth, after a mechanism analysis localizes a bottleneck, it must decide whether that bottleneck naturally yields a falsifiable intervention or whether the paper should honestly remain a measurement / identification / negative-boundary study. Complexity alone is not closure. These are parts of existing paper design, experiment design, and evidence checks; they do not independently approve a paper or authorize a run.")}</p></div><div class="preflight-lock"><b>${summary.controls||0}</b><span>${pick("横向方法学控制","cross-cutting controls")}</span></div></div><div class="methodology-control-grid">${cards}</div><div class="system-retro-note"><b>${pick("为什么没有再加一套新流程：","Why this did not create another workflow:")}</b> ${pick(`这四项检查直接放进现有步骤，因此新增独立后端组件=${summary.primary_components_added||0}、新增职责层=${summary.functional_layers_added||0}。机制—干预闭环也复用 Paper Story / Evidence gate：如果机制不支持新方法，系统允许诚实停在分析型论文，不会为了形式完整强行造算法。`,`The four checks are inserted into existing steps, so new standalone backend components=${summary.primary_components_added||0} and new responsibility layers=${summary.functional_layers_added||0}. Mechanism-to-intervention closure also reuses the Paper Story / Evidence gates: when the mechanism does not justify a new method, the system allows an honest analysis paper rather than manufacturing an algorithm for cosmetic completeness.`)}</div></section>`;
  };
})();
