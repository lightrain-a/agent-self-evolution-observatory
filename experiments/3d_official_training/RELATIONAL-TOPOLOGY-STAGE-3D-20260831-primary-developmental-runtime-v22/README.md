# RELATIONAL-TOPOLOGY-STAGE-3D — primary developmental runtime v22

This receipt advances the already-authorized official developmental training path without opening any scientific P1 outcome.

## What changed

- Recovered the frozen real corpus on host 52 after transport/materialization loss.
- Verified `IS-SUPPORT-12.jsonl`, `IS-SUPPORT-14.jsonl`, and `eligible_scenes.txt` against the exact content addresses from the original host-232 qualification.
- Replayed the frozen v3 corpus compiler on 52 because 232 and 52 cannot directly reach one another; replay output is byte-identical to the preregistered corpus hashes.
- Preserved the two pre-claim transport-failure child directories instead of deleting or overwriting them.
- Passed a fresh batch-128 `SGP-14` resource preflight on RTX 3090 with the same initial-state, first-batch, corpus, and config hashes as the frozen admission.
- Started `SGP-14` developmental training on GPU4 while the shared `BEDROOM-SG2SC-SHARED` decoder continues on GPU3.

## Oracle-R2 workload adjudication

`SGP-12` is **not** launched as mandatory work. Oracle R1/R2 already rejected the old pure support-causality interpretation because the two training regimes differ in relation-edge and token supervision dose. The frozen next scientific gate requires only:

1. complete the primary `SGP-14` endpoint checkpoint;
2. complete the one frozen shared SG2SC endpoint checkpoint;
3. keep validation/test and topology outcomes unopened;
4. then request separate validation-P1 authority for the count-3/4 matched CHAIN/HUB screen.

`SGP-12` remains secondary exposure-regime context only and can be added later only if the final paper prospectively declares a secondary interaction claim.

## Scientific boundary

- scientific outcomes: `0`;
- validation P1: not authorized;
- test: unopened;
- training loss: provenance/health only, never paper evidence;
- PORT-010: unchanged at `HOLD_EVIDENCE_REVIEW_BLOCKED / BLOCK_BAKE_IN`.

The current paper protocol is topology-first: count-3/4 matched CHAIN versus HUB within `SGP-14`, followed—only after separate P1 authority—by exact-identity oracle graph substitution through the identical frozen decoder with paired downstream randomness.
