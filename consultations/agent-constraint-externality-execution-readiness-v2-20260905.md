# Agent Constraint Externality — Confirmatory Execution Readiness V2

Date: 2026-09-05  
Scientific object: `AGENT-CONSTRAINT-EXTERNALITY-20260831`  
Status: **MECHANICALLY CLOSED — ALL EXECUTION AUTHORITY FALSE**

This note is a human-readable companion to:

`generated/agent-constraint-externality-confirmatory-execution-readiness-v2-20260905.json`

It does not authorize provider calls, scientific episodes, or paper-claim expansion.

## Why V2 exists

The older execution proposal correctly froze the scientific design, but its stage list predated the final pre-execution addendum. It therefore did not spell out three now-critical steps as standalone execution stages:

1. confirmatory reserve source-failure + repair-artifact creation;
2. topology-neutral `TARGET_ONLY_VERIFICATION` before topology treatment;
3. final panel freeze before any INDEPENDENT/LOW/HIGH collateral outcome.

V2 makes those steps explicit without changing the scientific estimand or workload envelope.

## Exact legal sequence after a future separate authority decision

1. **Provider readiness check** — non-scientific synthetic readiness only.
2. **Gate 0 clean direct capability** — exact actor/runtime qualification.
3. **Gate 1 Direct-SFQ-A0** — qualify a semantic repair-opportunity regime.
4. **Development repeat qualification** — six permanently excluded families; freeze `R* ∈ {2,3}` using within-condition stability only.
5. **Precision N* freeze** — on the same excluded development families, select `N* ∈ {12,16,20,24}` from conservative dispersion; no effect mean/sign is emitted or used.
6. **Confirmatory reserve freeze** — freeze exactly 24 reserve family IDs and the stable hash order.
7. **Confirmatory source + repair** — establish valid semantic source failures and freeze exactly one repair artifact per candidate before topology treatment.
8. **TARGET_ONLY_VERIFICATION** — on the topology-neutral target-only surface, use the exact frozen repair and common snapshot; uptake delta must be at least `+0.50` for eligibility.
9. **Confirmatory panel freeze** — select the first `N*` eligible families under frozen stable-hash order. Insufficient support causes STOP; do not shrink N.
10. **SHAM artifact/subset freeze** — freeze sham bytes and the preregistered subset before collateral outcomes.
11. **RQ1/RQ2 locked-panel execution** — collect the full prequalified INDEPENDENT/LOW/HIGH × NO_UPDATE/REAL_REPAIR panel once. No post-treatment target filtering or backfill.
12. **RQ1 analysis gate** — first adjudicate pooled update-attributable collateral externality and co-report topology-specific target-repair retention.
13. **RQ2 analysis** — only if RQ1 passes, analyze `UE_HIGH - UE_INDEPENDENT` and ordered/exposure profiles on the same already-frozen outcomes. No new RQ2 data collection.
14. **RQ3 held-out prediction** — only if RQ1/RQ2 pass; all ranking baselines reuse the same held-out outcomes.
15. **RQ4 GTCC** — only if RQ1/RQ2/RQ3 pass; Random-k, Same-App-k, and GTCC use identical probe budgets.

## Anti-bias invariants

- `TARGET_ONLY_VERIFICATION` precedes topology treatment.
- Once a family enters I/L/H, topology-specific target success/failure is an outcome, never an eligibility filter.
- RQ1/RQ2 data are collected once; only analysis is sequentially unlocked.
- Development effect direction cannot change `N*`.
- `R>3` is impossible.
- No post-topology reserve backfill.
- Nested branches/repeats/model calls never become extra family-level `n`.
- RQ3 cannot open after RQ1/RQ2 failure.
- RQ4 cannot open after RQ3 failure.
- A failed frozen claim is not rescued by adding models, families, seeds, or new baselines.

## Current authority

All remain `false`:

- provider readiness execution;
- provider/scientific execution;
- Gate 0;
- Gate 1;
- development repeat qualification;
- confirmatory source/repair;
- TARGET_ONLY_VERIFICATION;
- RQ1/RQ2 execution or analysis;
- RQ3;
- RQ4;
- secondary actor;
- external updater;
- paper claim expansion.

## Current next legal action

A future run requires an **explicit separate human authority decision**, initially only for the provider-readiness gate. Scientific dispatch remains closed until the required upstream gates pass under their own frozen contracts.
